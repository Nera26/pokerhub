- pattern: main
  required_status_checks:
    - ci
    - "ci / dr-runbook"
    - "ci / dr-trends"
    - "ci / workflow-sla"
    - contracts
    - "deploy / preflight"
    - "deploy / deploy"
    - check-proof-archive
    - "ci / ops-artifacts-verify"
    - spectator-retention
