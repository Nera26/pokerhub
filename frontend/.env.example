# Base URL for API requests (required when NODE_ENV is not 'development')
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Socket.IO server URL (required when NODE_ENV is not 'development')
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000

# Optional: log level for client output
NEXT_PUBLIC_LOG_LEVEL=info
