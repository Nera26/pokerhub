{
  "dashboard": {
    "title": "WebSocket Latency",
    "labels": {
      "pagerduty_service": "pokerhub-sre",
      "runbook": "docs/runbooks/websocket-latency.md"
    },
    "panels": [
      {
        "type": "timeseries",
        "title": "p95 latency",
        "datasource": "Prometheus",
        "targets": [
          { "expr": "histogram_quantile(0.95, sum(rate(ws_latency_bucket[5m])) by (le))" }
        ]
      }
    ]
  }
}
