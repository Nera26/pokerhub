apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: room-worker-pod-kill
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - default
    labelSelectors:
      app: room-worker
  duration: '30s'
  scheduler:
    cron: '*/5 * * * *'
